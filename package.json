{
  "name": "ipl-auction",
  "version": "1.0.0",
  "private": true,
  "description": "Live multiplayer IPL-style auction with Gemini-generated players, AI team verdict and WebRTC voice chat.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n SERVER,CLIENT -c magenta,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix server test",
    "test:game": "npm --prefix server run test:game",
    "test:all": "npm --prefix server run test:all"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
